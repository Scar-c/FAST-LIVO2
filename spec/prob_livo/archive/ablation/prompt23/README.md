# Prompt23 archive — ablation only

This directory preserves the Prompt23 Oxford/NTU LIVO-STRIDE sensitivity
artifacts for historical comparison. Prompt22 FEJ-OFF remains the canonical
production authority; none of the Prompt23 runner/config changes are loaded by
the canonical production path.

Source commits: `f0cd38c`, `d869a7a`, and `faba915` on the pre-cleanup
`prompt38-prob-sa-map-commit-forensic` history. The copied CSV values and the
shared stride YAML are unchanged. This archive is `ABLATION_ONLY` and
`NOT_CANONICAL`.
